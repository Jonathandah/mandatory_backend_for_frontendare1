import React from "react";
import { Redirect, Route, BrowserRouter as Router } from "react-router-dom";

function Chat() {
  return (
    <Router>
      <div className="Chat" />;
    </Router>
  );
}

export default Chat;
